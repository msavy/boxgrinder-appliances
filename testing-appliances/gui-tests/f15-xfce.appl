#Warning, this **does not work properly yet** due to some dependency issues (not BoxGrinder Build related). 
#I have determined that some necessary x-org packages needed aren't being installed.  However, even with these it isn't
#picking up the display quite right, and is terminating. So a bit more detective work is required. 
name: f15-xfce
summary: fedora-15 with XFCE                               
os:
  name: fedora
  version: 15
hardware:
  partitions:
    "/":
      size: 5
appliances:
  - _gui_base_systemd
packages:
  - @xfce_desktop
  - @critical-path-xfce